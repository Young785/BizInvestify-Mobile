- Add neccessary middlewares to where needed
- Add the roles permission to any part it is needed
- Maintain the colors and themes
- Always add the apis and not mock datas to anything you are creating, you cannot find any api then create for it
- Make sure all links, buttons are working
- Ensure allurity measure in programming are in right places to avoid vulnerability to hackers
- Do what is right Always
- Make sure you are following the steps and not skipping anything
- Always notes this is a website that should be able to access not only in my country but other countries. So make sure language, currencies and others are well implemented so don't hard code something like currencies or others
- Ensure anything you are doing is business wise and presentable with good ui ux and well understood by it users.
- Always improve the document that will make this project easy for artificaial i like you to easily create mobile app for this project with less stress. Always improve this evrytime a feature is done.
- Always log errors from back end to the frontend and if no message then add your default message like this for example {"success":false,"message":"Invalid credentials"}
- Also add the race condition limit and other security measures

- Maintain just one email notification templates for all

- ON the backend I want all places on the backend that needs notifications, activity logs not all controller should have it just the most important places user and admins need to keep record of this is neededFor example login from another ip address
- Sign up notification
- Use Paginations Toastr messages to log all needed response message
Listing Notification and so on

Create a well detailed roles and permission maybe you can use spate or gates so superadmin can easily manage other roles permissions and others

✅ SQL Injection Protection: Pattern detection and blocking
✅ XSS Protection: Script injection prevention
✅ Path Traversal Protection: Directory traversal blocking
✅ Command Injection Protection: System command blocking
✅ DoS Protection: Request size limits (100KB max)
✅ Bot Protection: Suspicious user-agent blocking
✅ Security Headers: Complete set of protective headers

✅ Failed Attempt Tracking: Per-user lockout system
✅ Device Fingerprinting: IP + User-Agent based tracking
✅ Session Management: Trusted device system
✅ Input Sanitization: Null byte and whitespace cleaning

All the best. Thanks